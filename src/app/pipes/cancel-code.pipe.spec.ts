import { CancelCodePipe } from './cancel-code.pipe';

describe('CancelCodePipe', () => {
  it('create an instance', () => {
    const pipe = new CancelCodePipe();
    expect(pipe).toBeTruthy();
  });
});
